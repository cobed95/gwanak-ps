def arrangeStudents(a, b):
    b = ["B"]*len(a)
    g = ["G"]*len(b)

    boys = {}
    values = a
    allinline = sorted(boyinline + girlinline)

    #Given the dictionary with Gender: Height sorted by heights, scan through the sorted dictionary looking for any
    #consecutive gender keys



    sol = "True"
    return sol